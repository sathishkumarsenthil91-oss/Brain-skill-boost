# Browser build: use public credentials only. These are optional for the existing project defaults.
VITE_SUPABASE_URL=https://nzgisrrrbabedlntmcoc.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
# Legacy public key compatibility:
VITE_SUPABASE_ANON_KEY=

# Put these in Supabase Edge Function secrets for hosted APIs, never in VITE_* variables.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Used only by the optional local Express server:
GEMINI_API_KEY=
